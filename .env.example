LOGIN_ID=your_id
LOGIN_PASSWORD=your_password
GEMINI_API_KEY=your_api_key

# Open the web UI in the default browser on startup (true/false).
# Automatically skipped on Linux without a GUI display (DISPLAY / WAYLAND_DISPLAY).
OPEN_BROWSER=true
